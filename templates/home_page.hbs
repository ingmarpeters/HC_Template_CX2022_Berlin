<h1 class="visibility-hidden">{{ help_center.name }}</h1>

<div class="container">
  <section class="section knowledge-base">
    <h2 class="visibility-hidden">{{ t 'categories' }}</h2>
    <section class="categories blocks">
      <ul class="cx2022-blocks-list">
        <!-- FAQ Entry -->
        {{#if settings.faq}}       
          {{#each categories}}
            {{#is name 'Allgemein'}}
              {{#each sections}}
                {{#is name 'FAQ'}}		
                  <li class="cx2022-blocks-item faq-block">
                    <a href='{{url}}' class="cx2022-blocks-item-link">
                      <span class="cx2022-blocks-item-title">
                        {{name}}
                      </span>
                    </a>
                  </li>
                {{/is}}
              {{/each}}
            {{/is}}
          {{/each}}
        {{/if}}
        
        <!-- Form 1 -->
        {{#if settings.form1_url}}
        	<li class="cx2022-blocks-item form1-block">
            <a href='{{settings.form1_url}}' class="cx2022-blocks-item-link">
              <span class="cx2022-blocks-item-title">
                {{settings.form1_title}}
              </span>
            </a>
        	</li>
      	{{/if}}
        
        <!-- Form 2 -->
        {{#if settings.form2_url}}
          <li class="cx2022-blocks-item form2-block">
            <a href='{{settings.form2_url}}' class="cx2022-blocks-item-link">
              <span class="cx2022-blocks-item-title">
                {{settings.form2_title}}
              </span>
            </a>            
        	</li>
      	{{/if}}
        
        <!-- Book Appointment -->
        {{#if settings.book_appointment_url}}
          <li class="cx2022-blocks-item appointment-block">
            <a href='{{settings.book_appointment_url}}' class="cx2022-blocks-item-link">
              <span class="cx2022-blocks-item-title">
                {{settings.book_appointment_title}}
              </span>
            </a>            
        	</li>  
      	{{/if}}
        
				<!-- Additional Entry --> 
        {{#if settings.additional_entry_url}}
          <li class="cx2022-blocks-item additional-entry-block">
            <a href='{{settings.additional_entry_url}}' class="cx2022-blocks-item-link">
              <span class="cx2022-blocks-item-title">
                {{settings.additional_entry_title}}
              </span>
            </a>            
        	</li>
      	{{/if}}
          
        </ul>
    </section>
  </section>    
</div>
